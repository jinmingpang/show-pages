import '../css/index.styl';
import '../lib/fullpage';

import pages from 'USER_ROOT_PATH/js/index.js';
import p from 'USER_ROOT_PATH/page.yaml';
import a from 'USER_ROOT_PATH/app.yaml';

console.log('pages----->', pages, p, a);

const init = () => {
  // alert(1)
};

init();
